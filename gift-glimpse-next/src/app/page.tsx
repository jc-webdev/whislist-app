// Cała treść tego widoku renderuje się w AppShell (src/app/layout.tsx),
// który sam odczytuje bieżącą ścieżkę. Ten plik istnieje tylko po to,
// żeby Next.js uznał "/" za prawidłową, routowalną stronę.
export default function Page() {
  return null;
}
